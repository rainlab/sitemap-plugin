# ===================================
#  Form Field Definitions
# ===================================

tabs:
    fields:

        cms_pages:
            commentAbove: Select which CMS pages you would like to appear in the sitemap
            type: partial
            path: cms_page_list
            tab: CMS
